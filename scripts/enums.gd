extends Node

enum EFFECT { DAMAGE, HEAL, SLOW, POISON }
### set types to be used alongside a float for intensity (in item resources)

enum TARGET_GROUP { player, enemy }
### this key name should be the exact same as the group name
